version: "3.9"

services:
  mongodb:
    image: mongo:7.0
    container_name: udaan-mongodb-dev
    restart: unless-stopped
    environment:
      MONGO_INITDB_ROOT_USERNAME: ${MONGO_USERNAME:-admin}
      MONGO_INITDB_ROOT_PASSWORD: ${MONGO_PASSWORD:-password}
      MONGO_INITDB_DATABASE: udaan
    ports:
      - "27017:27017"
    volumes:
      - mongodb_data_dev:/data/db

 
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: udaan-backend-dev
    restart: unless-stopped
    ports:
      - "5000:5000"
    environment:
      NODE_ENV: development
      PORT: 5000
      MONGO_URI: mongodb://${MONGO_USERNAME:-admin}:${MONGO_PASSWORD:-password}@mongodb:27017/udaan?authSource=admin
      JWT_SECRET: ${JWT_SECRET:-your-secret-key}
      SMTP_HOST: ${SMTP_HOST}
      SMTP_PORT: ${SMTP_PORT}
      SMTP_USER: ${SMTP_USER}
      SMTP_PASSWORD: ${SMTP_PASSWORD}
      FRONTEND_URL: ${FRONTEND_URL:-http://localhost}
      ADMIN_EMAIL: ${ADMIN_EMAIL:-admin@ngo.org}
      ADMIN_PASSWORD: ${ADMIN_PASSWORD:-admin123}
    depends_on:
      - mongodb

  
  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    container_name: udaan-frontend-dev
    restart: unless-stopped
    ports:
      - "80:80"
      - "3000:80"
    environment:
      VITE_API_URL: ${VITE_API_URL:-http://localhost:5000/api}
    depends_on:
      - backend

volumes:
  mongodb_data_dev:
